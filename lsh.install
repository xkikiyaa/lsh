post_install() {
if [ -f /etc/shells ] && ! grep -qx "/usr/bin/lsh" /etc/shells; then
echo "/usr/bin/lsh" >> /etc/shells
fi
}

post_upgrade() {
post_install
}

post_remove() {
if [ -f /etc/shells ]; then
sed -i '|^/usr/bin/lsh$|d' /etc/shells
fi
}
